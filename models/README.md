# Models Directory

This directory contains financial models, reusable templates, and generated model builds used throughout the course project.

## Structure

- `templates/`
  Stores reusable master templates and base modeling files.

- `builds/`
  Stores completed models, modified analyses, and scenario outputs generated during later stages.

## Notes

Templates should remain clean and reusable.  
